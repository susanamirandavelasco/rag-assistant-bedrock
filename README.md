# RAG Assistant over AWS Bedrock Documentation

A Retrieval-Augmented Generation (RAG) assistant built from scratch in Python.

## Goals

- Understand RAG architecture
- Reuse a semantic search engine
- Build retrieval and generation without LangChain
- Learn every component before using frameworks